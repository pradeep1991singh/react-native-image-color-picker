{
  "name": "react-native-image-color-picker",
  "version": "1.0.1",
  "description":
    "Image color picker based on image source provided and return image different color palettes or average color palette",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "precommit": "lint-staged && npm test",
    "prettify":
      "prettier --single-quote --trailing-comma es5 --write 'src/**/*.js'"
  },
  "repository": {
    "type": "git",
    "url":
      "git+https://github.com/pradeep1991singh/react-native-image-color-picker.git"
  },
  "keywords": [
    "image-color-picker",
    "image-color",
    "color-picker",
    "color-palette",
    "dominant-color-palette",
    "average-color-palette",
    "average-color",
    "color",
    "image"
  ],
  "author": "pradeep1991singh",
  "license": "ISC",
  "dependencies": {
    "react-native-fetch-blob": "^0.10.8"
  },
  "jest": {
    "preset": "react-native",
    "transformIgnorePatterns": [
      "node_modules/(?!(jest-)?react-native|react-navigation|jest-fetch-mock)"
    ],
    "setupFiles": ["<rootDir>/__mocks__/setup.js"]
  },
  "devDependencies": {
    "jest": "^21.2.1",
    "lint-staged": "^4.2.3",
    "prettier": "^1.7.4",
    "react": "^16.0.0",
    "react-native": "^0.49.3",
    "react-test-renderer": "^16.0.0"
  },
  "lint-staged": {
    "src/**/*.js": [
      "prettier --single-quote --trailing-comma es5 --write",
      "git add"
    ]
  }
}
